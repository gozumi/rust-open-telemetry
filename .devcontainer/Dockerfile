FROM ubuntu:latest

# RUN groupadd -r developer \
#     && useradd --no-log-init -r -g developer developer

# RUN mkdir /workspace
#     && chown developer:developer /workspace

# USER developer

# WORKDIR /workspace
